{
  "name": "parcel-vue-single-html-page",
  "version": "1.0.0",
  "description": "A parcel vue single html page",
  "scripts": {
    "dev": "parcel src/*.html",
    "build": "parcel build --no-minify ./src/*.html",
    "clean": "rm -rf dist"
  },
  "license": "MIT",
  "devDependencies": {
    "parcel-bundler": "^1.12.4"
  }
}
